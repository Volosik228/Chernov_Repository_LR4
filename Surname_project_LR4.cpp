#include <iostream>
using namespace std;

// ===== Объявление функций =====
void ввестиРазмерМассива(int &N);
void ввестиЭлементыМассива(int arr[], int N);
void найтиСуммуЭлементов(int arr[], int N);
void найтиСреднееЗначение(int arr[], int N);

// ===== Главная функция =====
int main() {
    int выбор;
    int N = 0;
    int массив[100]; // фиксированный размер массива

    do {
        // Вывод меню
        cout << "\nМЕНЮ:\n";
        cout << "1. Ввести размер массива N\n";
        cout << "2. Ввести значения элементов массива (N целых чисел)\n";
        cout << "3. Найти сумму всех элементов массива\n";
        cout << "4. Найти среднее значение элементов массива\n";
        cout << "0. Выход\n";
        cout << "Выберите пункт меню: ";
        cin >> выбор;

        // Обработка выбора через switch
        switch (выбор) {
            case 1:
                ввестиРазмерМассива(N);
                break;
            case 2:
                ввестиЭлементыМассива(массив, N);
                break;
            case 3:
                найтиСуммуЭлементов(массив, N);
                break;
            case 4:
                найтиСреднееЗначение(массив, N);
                break;
            case 0:
                cout << "Выход из программы.\n";
                break;
            default:
                cout << "Неверный выбор. Повторите попытку.\n";
        }

    } while (выбор != 0);

    return 0;
}

// ===== Реализация функций (пока пустые) =====

void ввестиРазмерМассива(int &N) {
    // TODO: Реализовать ввод размера массива
}

void ввестиЭлементыМассива(int arr[], int N) {
    // TODO: Реализовать ввод элементов массива
}

void найтиСуммуЭлементов(int arr[], int N) {
    // TODO: Реализовать подсчет суммы элементов массива
}

void найтиСреднееЗначение(int arr[], int N) {
    // TODO: Реализовать вычисление среднего значения элементов массива
}